import type { Options } from "./types";

const options: Options = {
    fit: false,
    cols: 10,
    rows: 10,
    disabledDirections: [],
    words: [],
    case: "upper",
    sections: 0,
    maxRetries: 10,
};

// Export the object
module.exports = options;